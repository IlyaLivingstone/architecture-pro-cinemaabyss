server:
  port: 8082
spring:
  application:
    name: events
  kafka:
    bootstrap-servers: ${KAFKA_BROKERS:kafka:9092}
management:
  endpoints:
    web:
      exposure:
        include: health,info,refresh
